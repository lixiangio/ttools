"use strict"

const get = require('../get.js')

/**
 * 合并一个或多个数组
 * 按条件合并两个数组，类似sql数据库的join操作，将两个数组通过公共键合并为一个数组。
 */
module.exports = function (data, options = {}) {

   for (let item of data) {

   }
   
   return data

}